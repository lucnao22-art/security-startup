// tailwind.config.js
/** @type {import('tailwindcss').Config} */
module.exports = {
    content: [
        './**/templates/**/*.html', // SỬA LẠI DÒNG NÀY
    ],
    theme: {
        extend: {},
    },
    plugins: [
        require('daisyui'), // Đảm bảo plugin daisyUI được thêm vào
    ],
}